

<%= form_for [current_user, @album] do |f| %>
<% if @album.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@album.errors.count, "error") %> prohibited this photo from being saved:</h2>
      <ul>
      <% @album.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>
 
  <p>
    <%= f.label :describtion %><br>
    <%= f.text_area :describtion %>
  </p>
 
  <p>
     <%= f.submit value ="create" ,{:class=>"savebutton"} %>
  </p>
<% end %>

